services:
  setup:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: select-setup
    working_dir: /var/www
    command: /usr/local/bin/setup.sh
    volumes:
      - .:/var/www
      - ./docker/php/local.ini:/usr/local/etc/php/conf.d/local.ini
    networks:
      - select
    depends_on:
      db:
        condition: service_healthy

  select:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: select-app
    restart: unless-stopped
    working_dir: /var/www
    volumes:
      - .:/var/www
      - ./docker/php/local.ini:/usr/local/etc/php/conf.d/local.ini
    networks:
      - select
    depends_on:
      db:
        condition: service_healthy
      setup:
        condition: service_completed_successfully

  nginx:
    image: nginx:alpine
    container_name: select-nginx
    restart: unless-stopped
    ports:
      - "${APP_PORT:-8000}:80"
    volumes:
      - .:/var/www
      - ./docker/nginx/default.conf:/etc/nginx/conf.d/default.conf
    networks:
      - select
    depends_on:
      - select

  db:
    image: postgres:16-alpine
    container_name: select-db
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${DB_DATABASE:-select}
      POSTGRES_USER: ${DB_USERNAME:-select}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-secret}
    ports:
      - "${DB_EXTERNAL_PORT:-5432}:5432"
    volumes:
      - select-db:/var/lib/postgresql/data
    networks:
      - select
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USERNAME:-select} -d ${DB_DATABASE:-select}"]
      interval: 5s
      timeout: 5s
      retries: 5

  reverb:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: select-reverb
    restart: unless-stopped
    working_dir: /var/www
    command: php artisan reverb:start --host=0.0.0.0 --port=8080
    ports:
      - "${FORWARD_REVERB_PORT:-8080}:8080"
    volumes:
      - .:/var/www
    networks:
      - select
    depends_on:
      setup:
        condition: service_completed_successfully

  queue:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: select-queue
    restart: unless-stopped
    working_dir: /var/www
    command: php artisan queue:work --sleep=3 --tries=3 --max-jobs=1000 --max-time=3600
    volumes:
      - .:/var/www
    networks:
      - select
    depends_on:
      setup:
        condition: service_completed_successfully

  scheduler:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: select-scheduler
    restart: unless-stopped
    working_dir: /var/www
    command: php artisan schedule:work
    volumes:
      - .:/var/www
    networks:
      - select
    depends_on:
      setup:
        condition: service_completed_successfully

  delectus:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: select-delectus
    restart: unless-stopped
    working_dir: /var/www
    command: php artisan delectus:run -v
    volumes:
      - .:/var/www
    networks:
      - select
    depends_on:
      setup:
        condition: service_completed_successfully
      reverb:
        condition: service_started

networks:
  select:
    driver: bridge

volumes:
  select-db:
